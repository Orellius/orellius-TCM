# ── Secrets & Environment ──
.env
.env.local
.env.*.local

# ── Python ──
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/
poetry.lock

# ── Rust / Tauri ──
desktop/src-tauri/target/

# ── Node ──
node_modules/
desktop/node_modules/
desktop/dist/
pnpm-lock.yaml

# ── Media output ──
media/

# ── Telegram sessions (CRITICAL — contains auth tokens) ──
*.session
*.session-journal

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# ── Docker volumes ──
pgdata/
redisdata/
qdrantdata/

# ── OS ──
Thumbs.db
._*

# ── Logs ──
*.log
.claude/
backend/data/
!backend/data/hfc_zones.json

# agent config
CLAUDE.md
AGENTS.md
.claude/

# Working documents. Untracked on purpose: a visibility flip exposes every commit
# ever made, so a doc deleted from HEAD is still public. README.md is the project.
docs/

# Working documents. Notes to myself, not documentation. A visibility flip
# exposes every commit ever made, so a doc deleted from HEAD is still public.
# docs/ alone was not enough: these four names live at the repo root.
docs/
/NORTH_STAR.md
/PROJECT_STATE.md
/ROADMAP.md
/BACKLOG.md
/checkpoints/
/plans/
/internal/
